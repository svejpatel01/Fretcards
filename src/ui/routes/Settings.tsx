import { RoutePlaceholder } from './RoutePlaceholder'

export function Settings() {
  return (
    <RoutePlaceholder
      title="Settings"
      description="A4 reference, input device, sensitivity, clarity threshold, stretch preference, and sharps/flats land here in Phase 5."
    />
  )
}
