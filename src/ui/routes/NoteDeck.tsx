import { RoutePlaceholder } from './RoutePlaceholder'

export function NoteDeck() {
  return (
    <RoutePlaceholder
      title="Note finder"
      description="Flashcard grading against the mic lands here in Phase 3."
    />
  )
}
